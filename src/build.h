/**
 * @file build.h
 * @brief Header file for build functions.
 */

#ifndef __build__
#define __build__

#include "compile.h"

/**
 * @brief Function to build assembly functions.
 *
 * This function is responsible for building the assembly functions
 * required for the program. It does not take any arguments and does
 * not return any value.
 */
void build_asm_fcts();

/**
 * @brief Function to build getint function.
 *
 * This function is responsible for building the getint function
 * required for the program. It does not take any arguments and does
 * not return any value.
 */
void build_getint();

/**
 * @brief Function to build putint function.
 *
 * This function is responsible for building the putint function
 * required for the program. It does not take any arguments and does
 * not return any value.
 */
void build_putint();

/**
 * @brief Function to build getchar function.
 *
 * This function is responsible for building the getchar function
 * required for the program. It does not take any arguments and does
 * not return any value.
 */
void build_getchar();

/**
 * @brief Function to build putchar function.
 *
 * This function is responsible for building the putchar function
 * required for the program. It does not take any arguments and does
 * not return any value.
 */
void build_putchar();

#endif