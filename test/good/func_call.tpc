int main(void){
    //char string[10];
    int resultat;
    //string[0] = 'a';
    resultat = caca(1, 0, 0) + caca(1, 0, 0);
    putint(resultat);
    //putchar(test(string));
    return 0;
}

char test(char str[]){
    return str[0];
}

int caca(int ok, int poulet, int enfant){
    int popo, oulala;
    int pipi;
    oulala = 4;
    pipi = ok + poulet + oulala - enfant;
    return pipi;
}